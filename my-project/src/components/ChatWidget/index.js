import ChatWidget from "./ChatWidget"; // Import the main ChatWidget component

// Export ChatWidget as the default export
export default ChatWidget;

// (Optional) Named exports for other components, if needed
export { default as ChatHeader } from "./ChatHeader";
export { default as ChatBody } from "./ChatBody";
export { default as ChatInput } from "./ChatInput";
